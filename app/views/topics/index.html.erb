<h1>When does the...</h1>

<table cellpadding="0" cellspacing="0">
  <thead>
    <tr>
      <th></th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>
  <tbody>
<% @topics.each do |topic| %>
  <tr>
    <td><%= link_to topic.title, topic %></td>
    <td><%= pluralize(topic.votes.length, "vote")%></td>
    <td><%= button_to 'Up Vote  ', votes_path(topic_id: topic.id), method: :post %></td>
    <td><%= link_to 'Make Disappear', topic, method: :delete, data: { confirm: 'Are you sure you want to be crazy?' } %></td>
  </tr>
<% end %>
</tbody>
</table>

<br />

<%= link_to 'New Topic', new_topic_path %>
